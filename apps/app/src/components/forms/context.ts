import { createFormHookContexts } from "@tanstack/react-form";

// biome-ignore format: preserve format
export const {
	fieldContext,
	formContext,
	useFieldContext,
	useFormContext,
} = createFormHookContexts();
